services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_PATH: /app/data/finance_bot.sqlite3
    volumes:
      - ./data:/app/data
    restart: unless-stopped